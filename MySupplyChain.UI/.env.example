# Copy this file to .env.local for local development
# .env.local is git-ignored and never committed

# Leave this blank in development — Vite's proxy handles /api → localhost:5001
# Set this to your Render API URL when building for production
# VITE_API_BASE_URL=https://mysupplychain-api.onrender.com/api
